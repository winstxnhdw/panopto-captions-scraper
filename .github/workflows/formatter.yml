name: Format

on:
  push:
    paths:
      - .github/workflows/formatter.yml
      - biome.json
      - package.json
      - src/**/*.ts
      - scripts/**/*.ts
      - tests/**/*.ts

permissions:
  contents: write

jobs:
  format:
    uses: winstxnhdw/actions/.github/workflows/format-biome.yml@main
